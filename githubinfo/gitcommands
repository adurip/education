
Genration of the keys : Ubuntu

ssh-keygen -t ed25519 -C "email_id"

eval "$(ssh-agent -s)"

ssh-add /home/sairam/.ssh/id_ed25519

cat ~/.ssh/id_ed25519.pub

add this to github (settings)


github commands:

Copying a branch

github branch -c githubcommands

Branches
  sivanandalahari
  homework
  textbooks_Q2Y22

git branch

git switch -c sivanandalahari
git add *
git status
git commit *
git push --set-upstream origin sivanandalahari
git push -d origin sivanandalahari
